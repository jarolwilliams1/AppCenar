async function ValidarDatos(datosLogin, res)
 {
    if(datosLogin.usuario == null || datosLogin.usuario == ' ' || datosLogin.password == null || datosLogin.password == ' ')
    {
        
    }
    
}

module.exports = {ValidarDatos}