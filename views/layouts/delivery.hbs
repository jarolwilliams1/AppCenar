<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>AppCenar | Delivery</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg app-navbar fixed-top">
        <div class="container-fluid px-3 px-lg-4">
            <a href="/delivery/home" class="brand-logo">
                <div class="logo-square" style="background: linear-gradient(135deg, #0288D1, #01579B);">AC</div>
                <div class="brand-text" style="color: #0288D1;">App<span>Cenar</span></div>
            </a>

            <button class="navbar-toggler border-0 shadow-none p-1" type="button" data-bs-toggle="collapse" data-bs-target="#appNavbarCollapse" aria-controls="appNavbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="appNavbarCollapse">
                <ul class="navbar-nav mx-auto nav-center-menu">
                    <li class="nav-item"><a class="nav-link-custom {{#if (eq activeTab 'asignaciones')}}active{{/if}}" href="/delivery/home">Asignaciones</a></li>
                    <li class="nav-item"><a class="nav-link-custom {{#if (eq activeTab 'perfil')}}active{{/if}}" href="/delivery/perfil">Mi Perfil</a></li>
                </ul>

                <div class="nav-user-area">
                    <div class="user-info-block">
                        <div class="user-name">{{#if sessionUser.nombre}}{{sessionUser.nombre}}{{else if sessionUser.usuario}}{{sessionUser.usuario}}{{else}}Delivery{{/if}}</div>
                        <span class="role-badge role-badge-delivery">DELIVERY</span>
                    </div>
                    <a href="/logout" class="btn-nav-logout">Salir</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="main-wrapper">
        {{{body}}}
    </main>

    {{> footer}}

    <script src="/js/bootstrap.bundle.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
